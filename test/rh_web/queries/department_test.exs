defmodule RhWeb.Queries.DepartmentTest do
  @moduledoc false
  use RhWeb.ConnCase, async: true

  describe "departments queries" do
    test "find departments by id, return departments" do
      response = true

      assert response == true
    end

    test "find all departments, should return a list of departments" do
      response = true

      assert response == true
    end
  end
end
